{
  "license": "MIT",
  "main": "build/bundle.js",
  "devDependencies": {
    "webpack": "^4.27.1",
    "webpack-cli": "^3.1.2"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "discord.js": "^11.4.2",
    "express": "^4.16.4",
    "moment": "^2.20.1",
    "opusscript": "^0.0.6"
  },
  "scripts": {
    "test": "echo n/a",
    "dist": "webpack --mode=development --env.production",
    "build": "webpack --mode=development --env.development",
    "build-container": "yarn run build && docker build -t robit .",
    "dev": "yarn run dist && node dist/bundle.js",
    "dev-config": "yarn run dist && node dist/bundle.js config.json",
    "dev-container": "yarn run build-container && docker run -p 8080:8080 -d robit"
  }
}
